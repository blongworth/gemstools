% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{co2_ppm_to_umol_per_l}
\alias{co2_ppm_to_umol_per_l}
\title{Convert wet CO2 mixing ratio (ppm) to dissolved CO2 concentration (µmol/L)}
\usage{
co2_ppm_to_umol_per_l(
  xco2_ppm,
  temp_c,
  sal_psu,
  pressure_mbar = 1013.25,
  density_kg_per_l = NULL
)
}
\arguments{
\item{xco2_ppm}{Numeric vector. Wet CO2 mixing ratio in ppm.}

\item{temp_c}{Numeric vector. Water temperature in degrees Celsius.}

\item{sal_psu}{Numeric vector. Practical salinity (PSU).}

\item{pressure_mbar}{Numeric vector. Total gas pressure in mbar. Default: 1013.25.}

\item{density_kg_per_l}{Optional numeric vector. Water density in kg/L. Default: 1.0 (approx).}
}
\value{
Numeric vector of dissolved CO2 concentration in µmol/L.
}
\description{
Implements the CO2-Pro manual Section 4.3 (partial pressure from mixing ratio and pressure)
and Section 4.4 (Weiss 1974 solubility) to convert wet \code{xCO2} in ppm to dissolved CO2
concentration in µmol/L.
}
\details{
The conversion follows:
\itemize{
\item pCO2(atm) = xCO2(ppm) * (P(mbar) / 1013.25) / 1e6
\item Weiss (1974) solubility:
ln(K0) = -60.2409 + 93.4517*(100/T) + 23.3585\emph{ln(T/100)
+ S}(0.023517 - 0.023656*(T/100) + 0.0047036*(T/100)^2),
where K0 has units mol kg^-1 atm^-1, T is Kelvin, and S is salinity in PSU (≈ ppt).
\item \link{CO2}_mol/kg = K0 * pCO2(atm); then mol/kg → mol/L via density (kg/L).
}

Assumptions:
\itemize{
\item \code{xco2_ppm} is a wet mixing ratio from CO2-Pro (no water vapor correction needed).
\item Pressure compensation is already applied to \code{xco2_ppm}; \code{pressure_mbar} is total gas pressure.
\item Salinity is practical salinity (PSU), equivalent to parts per thousand for Weiss.
}

Edge cases and notes:
\itemize{
\item For freshwater set \code{sal_psu = 0} (the Weiss seawater term drops out).
\item For higher precision, provide \code{density_kg_per_l} (default is 1 kg/L ~ 1 L/kg).
\item Ensure \code{pressure_mbar} reflects the detector headspace; default is 1013.25 mbar (1 atm).
}

References:
\itemize{
\item Weiss, R. F. (1974). Carbon dioxide in water and seawater: the solubility of a non-ideal gas.
Marine Chemistry, 2:203–215. https://doi.org/10.1016/0304-4203(74)90015-2
}
}
\examples{
# Surface seawater, T = 20 C, S = 35 PSU, xCO2 = 415 ppm, P = 1013.25 mbar
co2_ppm_to_umol_per_l(415, 20, 35)

# Freshwater lake, T = 10 C, S = 0, xCO2 = 500 ppm, standard pressure
co2_ppm_to_umol_per_l(500, 10, 0)

# With density (e.g., seawater ~1.025 kg/L)
co2_ppm_to_umol_per_l(415, 20, 35, density_kg_per_l = 1.025)
}
